import { FakeOptions } from './core/core';
/**
 * Generate a random abbreviation.
 *
 * @category text
 *
 * @example
 *
 * randAbbreviation()
 *
 * @example
 *
 * randAbbreviation({ length: 10 })
 *
 */
export declare function randAbbreviation<O extends FakeOptions = never>(options?: O): [O] extends [never] ? string : O["length"] extends number ? string[] : string;

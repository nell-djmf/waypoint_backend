import { FakeOptions } from './core/core';
export interface SportCategories extends FakeOptions {
    category?: 'olympic' | 'outdoor' | 'winterOlympic';
}
/**
 * Generate a random sports.
 *
 * @category sports
 *
 * @example
 *
 * randSports()
 *
 * @example
 *
 * randSports({ category : 'olympic' })
 *
 * @example
 *
 * randSports({ length: 10 })
 *
 */
export declare function randSports<Options extends SportCategories = never>(options?: Options): [Options] extends [never] ? string : Options["length"] extends number ? string[] : string;

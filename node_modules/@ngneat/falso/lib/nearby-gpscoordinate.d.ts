import { FakeOptions } from './core/core';
/**
 * Generate a random GPS coordinate.
 *
 * @category address
 *
 * @example
 *
 * randNearbyGPSCoordinate()
 *
 * @example
 *
 * randNearbyGPSCoordinate({ length: 10 })
 *
 */
export declare function randNearbyGPSCoordinate<Options extends FakeOptions = never>(options?: Options): [Options] extends [never] ? number[] : Options["length"] extends number ? number[][] : number[];

import { FakeOptions } from './core/core';
/**
 * Generate a random directory path.
 *
 * @category system
 *
 * @example
 *
 * randDirectoryPath()
 *
 * @example
 *
 * randDirectoryPath({ length: 10 })
 *
 */
export declare function randDirectoryPath<Options extends FakeOptions = never>(options?: Options): [Options] extends [never] ? string : Options["length"] extends number ? string[] : string;

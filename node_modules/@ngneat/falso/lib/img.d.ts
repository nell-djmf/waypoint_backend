import { FakeOptions } from './core/core';
declare type Category = 'animals' | 'arch' | 'nature' | 'people' | 'tech';
/**
 * Generate a random img.
 *
 * @category General
 *
 * @example
 *
 * randImg()
 *
 * @example
 *
 * randImg({ length: 10 })
 *
 */
export declare function randImg<Options extends FakeOptions & {
    width?: number;
    height?: number;
    category?: Category;
} = never>(options?: Options): [Options] extends [never] ? string : Options["length"] extends number ? string[] : string;
export {};

import { FakeOptions } from './core/core';
export interface IbanOptions extends FakeOptions {
    countryCode?: string;
}
/**
 * Generate a random IBAN number.
 *
 * @category finance
 *
 * @example
 *
 * randIban()
 *
 * @example
 *
 * randSwift({ countryCode: 'DE' }) // country code with ISO country code
 *
 * @example
 *
 * randIban({ length: 10 })
 *
 */
export declare function randIban<Options extends IbanOptions = never>(options?: Options): [Options] extends [never] ? string : Options["length"] extends number ? string[] : string;

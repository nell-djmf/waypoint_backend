import { FakeOptions } from './core/core';
interface PermissionOptions extends FakeOptions {
    numeric?: boolean;
}
/**
 * Generate a random permission.
 *
 * @category system
 *
 * @example
 *
 * randPermission()
 *
 * @example
 *
 * randPermission({ length: 10 })
 *
 * @example
 *
 * randPermission({ numeric: true })
 *
 */
export declare function randPermission<Options extends PermissionOptions = never>(options?: Options): ([Options] extends [never] ? number : Options["length"] extends number ? number[] : number) | ([Options] extends [never] ? string : Options["length"] extends number ? string[] : string);
export {};

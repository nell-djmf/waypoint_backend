import { FakeOptions } from './core/core';
/**
 * Generate random social network
 *
 * @category internet
 *
 * @example
 *
 * randSocial()
 *
 * @example
 *
 * randSocial({ length: 2 })
 *
 *
 */
export declare function randSocial<Options extends FakeOptions = never>(options?: Options): [Options] extends [never] ? {
    name: string;
    link: string;
} : Options["length"] extends number ? {
    name: string;
    link: string;
}[] : {
    name: string;
    link: string;
};

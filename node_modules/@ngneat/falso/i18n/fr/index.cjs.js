'use strict';

Object.defineProperty(exports, '__esModule', { value: true });

var seedRandom = require('seedrandom');

function _interopDefaultLegacy (e) { return e && typeof e === 'object' && 'default' in e ? e : { 'default': e }; }

var seedRandom__default = /*#__PURE__*/_interopDefaultLegacy(seedRandom);

var prng = seedRandom__default["default"]();
/**
 * Create a seed.
 *
 * @category general
 *
 * @example
 *
 * random()
 *
 */

function random() {
  return prng();
}

function fake(data, options) {
  var dataSource = Array.isArray(data) ? function () {
    return randElement(data);
  } : data;

  if ((options === null || options === void 0 ? void 0 : options.length) === undefined) {
    return dataSource(0);
  }

  return Array.from({
    length: options.length
  }, function (_, index) {
    return dataSource(index);
  });
}
function randElement(arr) {
  return arr[Math.floor(random() * arr.length)];
}

var data = [
	"Phalarope à bec étroit",
	"Hirondelle des falaises",
	"Tyran caouanne",
	"Grive aztèque",
	"Paruline à capuchon",
	"Milouin commun",
	"L'Oriole de Scott",
	"Albatros à sourcils noirs",
	"Perruche Moine",
	"Magnifique Colibri",
	"Bécasseau à large bec",
	"Mésange buissonnière",
	"Geai mexicain",
	"Harle à capuchon",
	"Cigogne des bois",
	"Mouette rosée",
	"Bécasseau vert",
	"Grue blanche"
];

/**
 * Generate a random bird.
 *
 * @category animals
 *
 * @example
 *
 * randBird()
 *
 * @example
 *
 * randBird({ length: 10 })
 *
 */

function randBird(options) {
  return fake(data, options);
}

exports.randBird = randBird;
